syntax = "proto3";

package log;

service LogService {
  rpc StreamLogs(stream LogEntry) returns (Ack);
}

message LogEntry {
  string service_name = 1;
  string level = 2;
  string message = 3;
  int64 timestamp = 4;
}

message Ack {
  string status = 1;
}
