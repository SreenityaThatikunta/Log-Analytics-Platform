apiVersion: v1
kind: Service
metadata:
  name: query-service
  namespace: log-analytics
spec:
  selector:
    app: query-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
