apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: log-analytics
spec:
  selector:
    app: elasticsearch
  ports:
    - name: http
      port: 9200
      targetPort: 9200
  clusterIP: None
