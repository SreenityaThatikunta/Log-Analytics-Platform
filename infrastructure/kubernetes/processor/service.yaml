apiVersion: v1
kind: Service
metadata:
  name: processor
  namespace: log-analytics
spec:
  selector:
    app: processor
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP

